# Frontend Deployment Configuration (Vite)
# Rename this file to .env and change the URL when you deploy the frontend
# and want to connect to a deployed backend.

VITE_API_URL=https://api.your-deployed-domain.com/api/v1
